{
  "name": "@trace/cli",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "trace": "./bin/trace"
  },
  "scripts": {
    "build": "tsc -b",
    "lint": "tsc -b",
    "test": "vitest run"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
