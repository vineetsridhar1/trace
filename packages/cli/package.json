{
  "name": "@trace/cli",
  "version": "0.0.2",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -b && esbuild src/main.ts --bundle --platform=node --format=esm --outfile=../../runtime/bin/trace.mjs && chmod +x ../../runtime/bin/trace.mjs",
    "lint": "tsc -b",
    "test": "vitest run"
  },
  "dependencies": {
    "@trace/gql": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^24.5.2",
    "esbuild": "^0.27.4",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
