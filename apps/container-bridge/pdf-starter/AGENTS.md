# Trace PDF session

This repository creates one print-ready PDF document. Read `docs/ai-guidance.md` before editing. Keep the artifact in `src/App.tsx`, retain the download control and `@media print` rules, and do not add a backend, database, or external integration.
