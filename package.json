{
  "name": "trace-monorepo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev:server": "pnpm --filter trace-server dev",
    "dev:web": "pnpm --filter trace-web dev",
    "dev:desktop": "pnpm --filter trace start"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@apollo/protobufjs",
      "@prisma/engines",
      "bcrypt",
      "electron",
      "electron-winstaller",
      "esbuild",
      "node-pty",
      "prisma"
    ]
  }
}
